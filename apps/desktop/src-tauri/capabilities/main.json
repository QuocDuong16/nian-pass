{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal core capability for the local Nian Pass main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-destroy"
  ]
}
