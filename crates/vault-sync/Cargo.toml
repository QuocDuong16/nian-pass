[package]
name = "vault-sync"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Provider-independent three-way KDBX merge engine for Nian Pass"

[dependencies]
kdbx = { path = "../kdbx" }
thiserror.workspace = true
vault-core.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true
