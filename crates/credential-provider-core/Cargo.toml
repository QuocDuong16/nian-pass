[package]
name = "credential-provider-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Platform-neutral credential matching and narrow secret retrieval for Nian Pass"

[dependencies]
kdbx = { path = "../kdbx" }
url.workspace = true
vault-core.workspace = true

[lib]
path = "src/lib.rs"

[lints]
workspace = true
